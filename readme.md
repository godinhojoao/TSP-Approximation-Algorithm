## TSP Metric

- Exact solution:
  1. brute force
  2. branch and bound
- Approximation algorithm (2-optimal)
  1.nearest insertion

## How to run:
- `make run`
OR
- `g++ -std=c++17 -O2 -I src src/main.cpp src/Graph/Graph.cpp -o main && ./main`
